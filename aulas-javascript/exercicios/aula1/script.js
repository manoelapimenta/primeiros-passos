function checarsePodeDirigir () {

    let idade = prompt ('Digite uma idade');
    
        if (idade >= 18){
        alert ('Pode Dirigir!');
        }
        else {
        alert ('Não Pode Dirigir!');
        }
}
checarsePodeDirigir ();